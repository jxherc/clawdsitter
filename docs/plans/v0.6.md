# v0.6 plan — CLI wiring + end-to-end

## Small prerequisite
`createWatcher().feed()` returns the detection plus `text` (the matched window) so the limit
handler can parse the reset time from real context, not just the matched token.

## Task v0.6-cli — `src/index.js` + `bin.js`
- `parseArgs(argv)` → `{ command, args, help?, version? }`.
- `makeOnData({ watcher, supervisor })` → PTY data handler (feed → busy? signalError : start
  handleLimit(text)/handleTransient). Returns a tag for testability.
- `createApp({ config, command, args, stdout, stdin, sleep, now, log, notify, spawn })` →
  `{ host, supervisor, watcher }`, inject wired to `host.write`.
- `main(argv)` → load config, build logger/notifier, createApp on real streams, resolve child exit code.
- `bin.js` shim → `main().then(process.exit)`.

Tests (>=8): parseArgs (empty→null command; passthrough args; --help; --version; -h/-v);
makeOnData (transient→handleTransient; limit→handleLimit with reset text; null→nothing;
busy→signalError; return tags).

## Task v0.6-e2e — `test/fake-claude.js` + `test/e2e.test.js`
Real ConPTY, tiny timings (`backoffSteps:['30ms']`, `healObserveSeconds:0.05`, fallback ~60ms).
Tests (>=4): transient → injects phrase → child prints RESUMED; limit(fallback) → waits → resumes;
idle/healthy → no inject (kill cleanly); injected phrase equals configured `resumePhrase`.

## TDD: RED → GREEN → lint → progress.json → FINAL full regression.
