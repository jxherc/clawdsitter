# v0.3 plan — Reset-time parser

## Task v0.3-resettime — `src/resetTime.js`
`parseReset(text, now)` → `Date | null`.
- Match `resets\s+(\d{1,2}):(\d{2})\s*(am|pm)?` (case-insensitive) on the text.
- Convert to 24h: `pm` & h<12 → +12; `am` & h==12 → 0. No suffix → take hour as-is (24h).
- Build a Date on `now`'s calendar day with that h:m:00; if `<= now`, add one day.
- No match / out-of-range → `null`.

## Tests (>=8)
future-today; passed→tomorrow; pm afternoon; 12am→00:xx; 12pm→noon; 24h no-suffix; full real
limit string parses 02:10; garbage→null; seconds zeroed; now==target→tomorrow.

## TDD: RED → GREEN → lint → progress.json.
